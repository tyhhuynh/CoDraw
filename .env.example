# .env.example
# Copy this file to .env and fill in your actual values

# WebSocket URL for connecting to the Cloudflare Worker
# 
# Development (local):
#   VITE_WS_URL=ws://localhost:8787
#
# Production (after deploying worker):
#   VITE_WS_URL=wss://your-worker.your-subdomain.workers.dev
#
# To get your worker URL:
#   1. Deploy: cd worker && npm run deploy
#   2. Copy the URL from Cloudflare dashboard
#VITE_WS_URL=wss://your-worker.your-subdomain.workers.dev